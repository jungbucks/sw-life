# sw-life 미구현 유닛 상세 스펙 (unit4 · unit5 · index)

> **후임 구현 모델용.** DESIGN_SPEC.md(팔레트·컴포넌트 문법)와 unit1~3.html(코드 문법 원본)을 먼저 읽을 것.
> 이 문서의 수치·구성은 설계 확정본이다 — 임의로 바꾸지 말고, 막히면 사용자에게 질문.
> 각 유닛 완성 후 헤드리스 프로브/스크린샷 검증 → 커밋 → **사용자 확인 후** 다음 진행 (unit3까지의 관례).

## unit4.html — 모의 실험하는 소프트웨어 (액센트: 로즈 --ua:#DB2777 / dark #BE185D / soft #FDEEF5)

성취기준 12소생04-01~04. 카드 4개 + 퀴즈 5문항.

### 4-1 시뮬레이션이란 — 안전한 "만약에?" 실험실 (04-01)
- 개념: 현실을 컴퓨터 안에 본떠 위험 없이 실험. 비유 = 비행 시뮬레이터(추락해도 아무도 안 다침).
- **구성요소 5종** 필수 명시(플로우 필 재사용): 개체(움직이는 대상) · 규칙(행동/상호작용) · 환경 · 시간 · 파라미터(조건 값).
- 가치 리스트: 위험 0 / 비용 ↓(충돌 시험) / 시간 압축(100년 기후를 1분에) / 무한 반복(조건만 바꿔 재실험).

### 4-2 어디에 쓰이나 (04-02)
- 사례 카드 6종(.fusion 카드 재사용): 비행 조종 훈련 / 자동차 충돌 시험 / 일기예보(수치예보 모델) / 도시 교통·신호 / 게임 물리 엔진 / 방역 정책(4-3 예고).
- 활동 박스: "우리 학교에서 시뮬레이션으로 미리 실험해 보고 싶은 것 = ___ (예: 축제 부스 배치별 혼잡도)".

### 4-3 감염 확산 시뮬레이터 — 시그니처 LIVE (04-03 모델 표현 + 04-04 구현)
**canvas 기반. 사양 고정:**
- canvas: 부모 100% 폭 × 260px. 배경 #fff, 테두리 var(--line).
- 개체: N=80, 반지름 5px, 초기 위치 랜덤, 속도 vx,vy ∈ [-1.2, 1.2] px/frame, 벽 반사.
- 상태·색: S 건강 `#B9C4D2` / I 감염 `--ua(로즈)` / R 회복·면역 `#16A34A`. 색+범례 텍스트 병행(색 단독 금지).
- 규칙: 초기 감염 3마리. 두 개체 거리 < 12px이면 프레임당 확률 p로 S→I. 감염 280프레임 후 I→R.
- 시나리오 버튼 3종(btn ghost, 선택 시 .on) + [▶ 실행/다시]:
  ① 그대로 두기: p=0.08, 백신 0%  ② 마스크·거리두기: p=0.025, 백신 0%  ③ 백신 접종 50%: p=0.08, 시작 시 40마리 R.
- HUD(demo-note 위 한 줄): 건강 n · 감염 n · 회복 n · **최대 동시 감염(피크) n** — 피크가 핵심 교육 지표.
- 종료(I=0) 시 demo-note 판정: 피크 수치 제시 + "다른 시나리오와 피크를 비교해 보자 — 방역 뉴스의 <b>곡선 평탄화</b>가 바로 이 실험이다."
- **"이 시뮬의 설계도" 표** (04-03의 '원리를 모델로 표현'을 명시적으로 커버): 행 = 개체(점 80개)/규칙(접촉 시 확률 감염, 280프레임 후 회복)/파라미터(감염 확률 p, 백신 접종률) — "현실을 이 세 줄로 요약한 것이 모델".
- warn 오개념: **모델은 단순화다** — 잠복기·재감염·연령이 없다. "모델은 완벽하지 않지만 유용하다. 단, 무엇을 뺐는지 아는 사람만 결과를 바르게 읽는다."
- rAF 루프는 실행 버튼으로만 시작(자동재생 금지), reduced-motion 사용자 배려 문구 1줄.

### 4-4 나의 시뮬레이션 구상 (활동)
- 활동 박스 틀: "현상 ___ 을 시뮬레이션한다면 — 개체는 ___ , 규칙은 ___ , 바꿔 볼 파라미터는 ___" (예시: 매점 대기줄 — 개체=학생, 규칙=줄서기·계산 30초, 파라미터=계산대 수).

### 퀴즈 5문항 (정답 az 다양화)
① 시뮬레이션 개념(위험 없이 '만약에' 실험) ② 구성요소 중 '규칙'의 예 판별 ③ 가치(시간 압축) ④ 파라미터를 바꿔 보는 이유(=조건 실험이 시뮬의 본질) ⑤ 모델의 한계(단순화 — 뺀 것을 알아야 바르게 읽음).

## unit5.html — 가치를 창출하는 소프트웨어 (액센트: 그린 --ua:#16A34A / dark #15803D / soft #EAF7EE)

성취기준 12소생05-01~04. **워크북 성격 절반** — 데이터 과학 unit4(프로젝트 워크북)의 입력+복사 UX 재사용.

### 5-1 스타트업 — 불편이 가치가 되는 순간 (05-01)
- 개념: 스타트업 = 세상의 불편을 SW로 풀어 새 가치를 만드는 작은 팀. 사례 카드 3종(특정 기업명 대신 구조로): 음식 배달 중개(전화 주문의 불편) / 간편 송금(공인인증 불편) / 중고 거래(동네 직거래 불신).
- 공통 패턴 표: 오래된 불편 + SW의 강점(중개·자동화·신뢰 장치) = 새 가치. "1단원 융합의 공식과 같은 구조".

### 5-2 사용자 요구 분석 (05-02)
- 핵심: 아이디어보다 **불편을 겪는 사람** 먼저. 인터뷰 틀 4질문: 누가/언제·어디서/왜 불편한가/지금은 어떻게 버티는가.
- warn 오개념: "내가 만들고 싶은 것 ≠ 사용자가 필요한 것" — 요구 분석 없는 개발이 스타트업 실패 1순위.
- ✍️ 워크시트 입력 2칸: 타깃 사용자 / 인터뷰로 확인한 불편.

### 5-3 아이디어 캔버스 (05-02~03, 협력 설계)
- ✍️ 입력 그리드: 문제(한 문장) / 해결 아이디어(한 문장) / 핵심 기능 3개 / 역할 분담(누가 무엇을 — 협력 명시).
- tip: 기능은 3개로 제한하는 이유 = MVP(최소 기능 제품) — "다 만들기 전에 핵심만 만들어 검증".

### 5-4 3관점 평가 + 제출 (05-04)
- 평가 체크리스트(unit4 데과의 chk 컴포넌트): **사회적**(누구에게 이롭고, 누구에게 불리해질 수 있나) / **기능적**(핵심 기능이 약속대로 작동하나) / **윤리적**(개인정보·편향·중독 유도 요소 점검) — 각 관점 ✍️ 입력 1칸.
- 📋 결과 요약 복사(제출용): 팀명/팀원 + 5-2~5-4 입력 전체를 정리 텍스트로 (데과 unit4 buildText 패턴).
- 퀴즈 5문항: ① 스타트업 개념 ② 요구 분석이 먼저인 이유 ③ MVP ④ 3관점 평가 판별 ⑤ 가치의 윤리적 관점 사례.

## index.html — 시리즈 허브

- data-science/index.html 구조 이식(unit 카드 + .u1~.u5 액센트 변수), 도트 대신 **회로 배선 헤더** 모티프(unit1~3 header::before 재사용).
- 카드 5장 + "각 단원은 파일 하나로 완결(오프라인 OK)" 문구 + 자매 시리즈 링크 1줄(데이터 과학 시리즈).

## 완료 후

- DESIGN_SPEC.md 상태 표 갱신 (예정 → 제작), 메모리(sw-life-series) 갱신.
- 배포는 사용자 지시 대기 (GitHub repo 미생성 상태).
