# 소프트웨어와 생활 시리즈 — 유지보수 플레이북 (후임 모델용 진입점)

> 융합선택 '소프트웨어와 생활' 5단원 학습 페이지. **데이터 과학 시리즈의 형제** — 문법 계승, 무드 교체(웜 아이보리+회로 배선).
> 읽기 순서: 이 문서 → `DESIGN_SPEC.md`(팔레트·컴포넌트) → **`UNIT_SPECS.md`(unit4·5·index 상세 설계 — 확정본)** → unit1~3.html(코드 원본).

## 상태 (2026-07-11 갱신)

- ✅ **시리즈 전체 완성** — unit1(틸)·unit2(오렌지)·unit3(블루)·unit4(시뮬레이터, 로즈)·unit5(워크북, 그린)·index 허브. UNIT_SPECS.md 사양대로 구현 완료.
- ✅ 이후 보강 2건: **성취기준 해설(가) 반영**(f857573 — 부품 유연 활용·데이터 플랫폼 표·정제·화면구성/기능 계획) · **활동 빈칸 22개를 장식 span → 실제 input으로**(사용자 지적).
- 미배포 (GitHub repo 미생성). 배포 시 data-science와 동일 파이프라인(Pages → 정글 앱스토어) — 사용자 지시 대기.

## 절대 규칙

1. 팔레트 5색은 dataviz 검증 완료(`DESIGN_SPEC.md`) — 새 hex 금지, 단원 액센트는 `--ua` 3종 토큰 교체로만.
2. 성취기준 코드([12소생…]) 페이지 미표기. 진도 localStorage 미사용. 15px 미만 본문 금지. reduced-motion 대응.
3. **각 유닛 완성 → 검증 → 커밋 → 사용자 OK 후 다음 유닛** (unit1~3에서 확립된 관례).
4. unit3의 데이터 파트는 의도적으로 라이트 — 심화는 데이터 과학 시리즈 링크로 위임(중복 제작 금지). 윤리가 이 과목의 주인공.
5. 순수 HTML/CSS/JS 단일 파일, 오프라인 더블클릭 동작. 컴포넌트는 기존 유닛 모방 우선(창작 금지).

## 작업 절차

수정 → 헤드리스 dump-dom으로 JS 실행 확인(+시각 변경 시 스크린샷) → 커밋(한국어) → 사용자 보고. 검증 명령 예시는 data-science/CLAUDE.md와 동일.
